using DAL;
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Web;
using System.Web.UI;
using System.Web.UI.HtmlControls;
using System.Web.UI.WebControls;
using BAL;
using System.Reflection;
using BusinessLayer;
using System.IO;
using System.Globalization;
using System.Collections;
using Telerik.Web.UI;
using System.Web.Services;
using Newtonsoft.Json;
namespace AstralFFMS
{
    public partial class DashboardPresentMember : System.Web.UI.Page
    {
        string roleType = "", total = "";
        DataTable dtEmployee = null;
        string sql = "";
        string secondarySql = "";
        string PrimarySql = "";
        string UnApprovedSql = "";
        protected void Page_Load(object sender, EventArgs e)
        {
            if (!IsPostBack)
            {
                FromDate.Text = Request.QueryString["Date"];
                this.fillAllRecord();
            }
        }
        public void fillAllRecord()
        {
            roleType = Settings.Instance.RoleType;
            //if (roleType.Equals("Admin")) sql = "select a.smid,a.smname,a.mobile,b.smname as reportingPerson,tv.frtime1 as InTime,tv.totime1 as outTime,(select top 1 case address when '' then 'No Address Found' when null  then 'No Address Found' else address end as address  from (select  isnull (address,'No Address Found') as Address,lat_long_datetime  from TransVisit where vdate = replace(convert(NVARCHAR, DateAdd(minute,330,'" + FromDate.Text + "'), 106), ' ', '/') and smid = a.smid  union all select isnull (address,'No Address Found') as Address,lat_long_datetime from  temp_transdemo where vdate = replace(convert(NVARCHAR, DateAdd(minute,330,'" + FromDate.Text + "'), 106), ' ', '/') and smid = a.smid union all select  isnull (address,'No Address Found') as Address,lat_long_datetime  from distributercollection where vdate = replace(convert(NVARCHAR, DateAdd(minute,330,'" + FromDate.Text + "'), 106), ' ', '/') and smid = a.smid  union all select  isnull (address,'No Address Found') as Address,lat_long_datetime  from  temp_transvisitdist where vdate = replace(convert(NVARCHAR, DateAdd(minute,330,'" + FromDate.Text + "'), 106), ' ', '/')  and smid = a.smid   union all  select  isnull (address,'No Address Found') as Address,lat_long_datetime  from  temp_transorder where vdate = replace(convert(NVARCHAR, DateAdd(minute,330,'" + FromDate.Text + "'), 106), ' ', '/')  and smid = a.smid  union all select  isnull (address,'No Address Found') as Address,lat_long_datetime  from  temp_Transdemo where vdate = replace(convert(NVARCHAR, DateAdd(minute,330,'" + FromDate.Text + "'), 106), ' ', '/')   and smid = a.smid  union all select  isnull (address,'No Address Found') as Address,lat_long_datetime  from  temp_transcompetitor where vdate = replace(convert(NVARCHAR, DateAdd(minute,330,'" + FromDate.Text + "'), 106), ' ', '/')  and smid = a.smid   union all select  isnull (address,'No Address Found') as Address,lat_long_datetime  from  temp_transorder1 where vdate = replace(convert(NVARCHAR, DateAdd(minute,330,'" + FromDate.Text + "'), 106), ' ', '/')  and smid = a.smid   union all select  isnull (address,'No Address Found') as Address,lat_long_datetime  from  temp_TransFailedVisit where vdate = replace(convert(NVARCHAR, DateAdd(minute,330,'" + FromDate.Text + "'), 106), ' ', '/')  and smid = a.smid  union all select  isnull (address,'No Address Found') as Address,lat_long_datetime  from  temp_transdiststock where vdate = replace(convert(NVARCHAR, DateAdd(minute,330,'" + FromDate.Text + "'), 106), ' ', '/')  and smid = a.smid  union all select  isnull (address,'No Address Found') as Address,lat_long_datetime  from  temp_transcollection where vdate = replace(convert(NVARCHAR, DateAdd(minute,330,'" + FromDate.Text + "'), 106), ' ', '/') ) a order by lat_long_datetime desc) as Location from mastsalesrep a left join mastsalesrep b on a.underid=b.smid left join transvisit tv on a.smid=tv.smid  where a.smname not in ('.') and a.smid in (select  smid from TransVisit with (nolock) where vdate = replace(convert(NVARCHAR, DateAdd(minute,330,'" + FromDate.Text + "'), 106), ' ', '/')  and IsNull(appstatus,'DSR') !='Reject') and tv.vdate=replace(convert(NVARCHAR, DateAdd(minute,330,'" + FromDate.Text + "'), 106), ' ', '/') Order By a.smname";
            //else sql = "select a.smid,a.smname,a.mobile,b.smname as reportingPerson,tv.frtime1 as InTime,tv.totime1 as outTime,(select top 1 case address when '' then 'No Address Found' when null  then 'No Address Found' else address end as address  from (select  isnull (address,'No Address Found') as Address,lat_long_datetime  from TransVisit where vdate = replace(convert(NVARCHAR, DateAdd(minute,330,'" + FromDate.Text + "'), 106), ' ', '/') and smid = a.smid  union all select isnull (address,'No Address Found') as Address,lat_long_datetime from  temp_transdemo where vdate = replace(convert(NVARCHAR, DateAdd(minute,330,'" + FromDate.Text + "'), 106), ' ', '/') and smid = a.smid union all select  isnull (address,'No Address Found') as Address,lat_long_datetime  from distributercollection where vdate = replace(convert(NVARCHAR, DateAdd(minute,330,'" + FromDate.Text + "'), 106), ' ', '/') and smid = a.smid  union all select  isnull (address,'No Address Found') as Address,lat_long_datetime  from  temp_transvisitdist where vdate = replace(convert(NVARCHAR, DateAdd(minute,330,'" + FromDate.Text + "'), 106), ' ', '/')  and smid = a.smid   union all  select  isnull (address,'No Address Found') as Address,lat_long_datetime  from  temp_transorder where vdate = replace(convert(NVARCHAR, DateAdd(minute,330,'" + FromDate.Text + "'), 106), ' ', '/')  and smid = a.smid  union all select  isnull (address,'No Address Found') as Address,lat_long_datetime  from  temp_Transdemo where vdate = replace(convert(NVARCHAR, DateAdd(minute,330,'" + FromDate.Text + "'), 106), ' ', '/')   and smid = a.smid  union all select  isnull (address,'No Address Found') as Address,lat_long_datetime  from  temp_transcompetitor where vdate = replace(convert(NVARCHAR, DateAdd(minute,330,'" + FromDate.Text + "'), 106), ' ', '/')  and smid = a.smid   union all select  isnull (address,'No Address Found') as Address,lat_long_datetime  from  temp_transorder1 where vdate = replace(convert(NVARCHAR, DateAdd(minute,330,'" + FromDate.Text + "'), 106), ' ', '/')  and smid = a.smid   union all select  isnull (address,'No Address Found') as Address,lat_long_datetime  from  temp_TransFailedVisit where vdate = replace(convert(NVARCHAR, DateAdd(minute,330,'" + FromDate.Text + "'), 106), ' ', '/')  and smid = a.smid  union all select  isnull (address,'No Address Found') as Address,lat_long_datetime  from  temp_transdiststock where vdate = replace(convert(NVARCHAR, DateAdd(minute,330,'" + FromDate.Text + "'), 106), ' ', '/')  and smid = a.smid  union all select  isnull (address,'No Address Found') as Address,lat_long_datetime  from  temp_transcollection where vdate = replace(convert(NVARCHAR, DateAdd(minute,330,'" + FromDate.Text + "'), 106), ' ', '/') ) a  order by lat_long_datetime desc) as Location from mastsalesrep a left join mastsalesrep b on a.underid=b.smid  right join transvisit tv on a.smid=tv.smid where a.smname not in ('.') and " + Settings.Instance.SMID + " in (select  smid  from TransVisit with (nolock) where vdate = replace(convert(NVARCHAR, DateAdd(minute,330,'" + FromDate.Text + "'), 106), ' ', '/') and SMId in (select SMId from mastsalesrep with (nolock) where smid in (select smid from mastsalesrepgrp with (nolock)  where maingrp = " + Settings.Instance.SMID + ") and active=1 and smid<> " + Settings.Instance.SMID + " ) and IsNull(appstatus,'DSR') !='Reject') and tv.vdate=replace(convert(NVARCHAR, DateAdd(minute,330,'" + FromDate.Text + "'), 106), ' ', '/') Order By a.smname";
            sql = "select a.smid,a.smname,(select top 1 case smname when '' then '' when null  then '' else smname end as WithWhom  from (Select ms.smname from Transvisit tr left join MastSalesRep ms on ms.smid=tr.WithUserId where tr.vdate = replace(convert(NVARCHAR, DateAdd(minute,330,'" + FromDate.Text + "'), 106), ' ', '/') and tr.smid = a.smid  ) a ) as WithWhom,a.mobile,b.smname as reportingPerson,tv.frtime1 as InTime,case when tv.Lock=1 then tv.totime1 else '' end as outTime,(select top 1 case address when '' then 'No Address Found' when null  then 'No Address Found' else address end as address  from (select  isnull (address,'No Address Found') as Address,lat_long_datetime  from TransVisit where vdate = replace(convert(NVARCHAR, DateAdd(minute,330,'" + FromDate.Text + "'), 106), ' ', '/') and smid = a.smid  union all select isnull (address,'No Address Found') as Address,lat_long_datetime from  temp_transdemo where vdate = replace(convert(NVARCHAR, DateAdd(minute,330,'" + FromDate.Text + "'), 106), ' ', '/') and smid = a.smid union all select  isnull (address,'No Address Found') as Address,lat_long_datetime  from distributercollection where vdate = replace(convert(NVARCHAR, DateAdd(minute,330,'" + FromDate.Text + "'), 106), ' ', '/') and smid = a.smid  union all select  isnull (address,'No Address Found') as Address,lat_long_datetime  from  temp_transvisitdist where vdate = replace(convert(NVARCHAR, DateAdd(minute,330,'" + FromDate.Text + "'), 106), ' ', '/')  and smid = a.smid   union all  select  isnull (address,'No Address Found') as Address,lat_long_datetime  from  temp_transorder where vdate = replace(convert(NVARCHAR, DateAdd(minute,330,'" + FromDate.Text + "'), 106), ' ', '/')  and smid = a.smid  union all select  isnull (address,'No Address Found') as Address,lat_long_datetime  from  temp_Transdemo where vdate = replace(convert(NVARCHAR, DateAdd(minute,330,'" + FromDate.Text + "'), 106), ' ', '/')   and smid = a.smid  union all select  isnull (address,'No Address Found') as Address,lat_long_datetime  from  temp_transcompetitor where vdate = replace(convert(NVARCHAR, DateAdd(minute,330,'" + FromDate.Text + "'), 106), ' ', '/')  and smid = a.smid   union all select  isnull (address,'No Address Found') as Address,lat_long_datetime  from  temp_transorder1 where vdate = replace(convert(NVARCHAR, DateAdd(minute,330,'" + FromDate.Text + "'), 106), ' ', '/')  and smid = a.smid   union all select  isnull (address,'No Address Found') as Address,lat_long_datetime  from  temp_TransFailedVisit where vdate = replace(convert(NVARCHAR, DateAdd(minute,330,'" + FromDate.Text + "'), 106), ' ', '/')  and smid = a.smid  union all select  isnull (address,'No Address Found') as Address,lat_long_datetime  from  temp_transdiststock where vdate = replace(convert(NVARCHAR, DateAdd(minute,330,'" + FromDate.Text + "'), 106), ' ', '/')  and smid = a.smid  union all select  isnull (address,'No Address Found') as Address,lat_long_datetime  from  temp_transcollection where vdate = replace(convert(NVARCHAR, DateAdd(minute,330,'" + FromDate.Text + "'), 106), ' ', '/') and smid = a.smid ) a order by lat_long_datetime desc) as Location,case when a.Active='1' then 'Yes' else 'No' end as Active from mastsalesrep a left join mastsalesrep b on a.underid=b.smid left join transvisit tv on a.smid=tv.smid  WHERE a.smid IN (select smid from mastsalesrepgrp with (nolock)  where maingrp = " + Settings.Instance.SMID + ") and a.smid<> " + Settings.Instance.SMID + " and a.smid in (select  smid from TransVisit with (nolock) where vdate = replace(convert(NVARCHAR, DateAdd(minute,330,'" + FromDate.Text + "'), 106), ' ', '/')  and IsNull(appstatus,'DSR') !='Reject') and tv.vdate=replace(convert(NVARCHAR, DateAdd(minute,330,'" + FromDate.Text + "'), 106), ' ', '/') Order By a.smname";
            
            DataTable dt = DbConnectionDAL.GetDataTable(CommandType.Text, sql);
            rpt.DataSource = dt;
            rpt.DataBind();
        }
        protected void FromDate_TextChanged(object sender, EventArgs e)
        {
            try
            {
                System.Web.UI.ScriptManager.RegisterStartupScript(this, GetType(), "showspinner", "showspinner();", true);
                this.fillAllRecord();
                System.Web.UI.ScriptManager.RegisterStartupScript(this, GetType(), "hidespinner", "hidespinner();", true);
            }
            catch(Exception ex)
            { ex.ToString(); }
        }

    }
}